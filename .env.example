# 复制为 .env 并填写实际值

PORT=9080

# 必填。生成方式见 DEPLOY.md
# 重要：Argon2 哈希含 $，在 .env 中每个 $ 必须写成 $$，否则 docker compose 会截断哈希导致登录失败
# 示例（占位，勿直接使用）：
# APP_PASSWORD_HASH=$$argon2id$$v=19$$m=19456,t=2,p=1$$REPLACE$$ME
APP_PASSWORD_HASH=

STATIC_DIR=/app/dist
SECURE_COOKIES=true
DEFAULT_ZIP=150-0001
