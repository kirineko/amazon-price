# 复制为 .env 并填写实际值

# 应用监听端口（容器内 + 映射到宿主机 127.0.0.1 的同一端口）
# 默认 9080，避开已被占用的 8080/8000
PORT=9080

APP_PASSWORD_HASH=$argon2id$v=19$m=19456,t=2,p=1$REPLACE_ME

# 容器内固定为 /app/dist，本地开发可设为 dist
STATIC_DIR=/app/dist

# 经 HTTPS 访问时必须 true
SECURE_COOKIES=true

DEFAULT_ZIP=150-0001
